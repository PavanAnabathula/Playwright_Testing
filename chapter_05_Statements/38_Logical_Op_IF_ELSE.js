let user_id = 'admin'
let password = 'Secure123'
let Account_blocked = true

if (user_id === 'admin' && password === 'Secure123' && !Account_blocked) {
  console.log('Logged in Successfully')
} else {
  console.log('Account is blocked')
}
